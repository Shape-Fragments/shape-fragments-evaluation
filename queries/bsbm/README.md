Queries generated by:
```
./testdriver -runs 1 -w 0 -seed 22 http://172.17.0.2:3030/sparql #seed 22 chosen as it is the lowest seed where each query has results
IFS='
'
i=1; for query in $(cat logs.txt | grep 'Query = ' | cut -d '=' -f 2); do echo $query >> query$i.rq; i=$(($i+1)) ; done #logs.txt contains a copy of the log output of the fuseki server after running the previous command
```
